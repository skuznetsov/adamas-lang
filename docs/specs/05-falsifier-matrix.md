# Crystal V2 Falsifier Matrix

> Status: Draft v0.1, 2026-05-08.
> Companion to: `docs/specs/*.md`, `TODO.md`, `LANDMARKS.md`.
> Format: each row maps a normative claim to the smallest known falsifier.

## 1. Status Legend

- `[FALSIFIABLE]`: guard exists or the falsifier command is explicit.
- `[FRONTIER]`: known boundary; not yet fixed.
- `[MISSING-FALSIFIER]`: claim is important but lacks a narrow guard.
- `[REFUTED]`: branch tried and recorded as not a valid fix.

Each non-refuted row has a phase pressure:

- `current`: must be resolved before claiming the active frontier fixed.
- `next-touch`: must be resolved when code in that contract family changes.
- `pre-s2-clean`: must be resolved before declaring `s1 -> s2b` clean.
- `later`: useful but not on the active bootstrap gate.

`[MISSING-FALSIFIER]` rows without phase pressure are invalid.

## 2. Bootstrap Corridor

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| B1 | Produced stage must be built through `scripts/run_safe.sh`. | `00-bootstrap-contract.md` section 4 | Review/CI command check; direct produced binary execution is protocol violation. | current | [FALSIFIABLE] |
| B2 | A moved frontier is acceptable only with a guard and a named residual boundary. | `00-bootstrap-contract.md` section 5 | Commit lacks guard or TODO/LANDMARK boundary for a claimed fix. | current | [FALSIFIABLE] |
| B3 | Original-vs-stage semantic oracle is required when a change touches language behavior. | `00-bootstrap-contract.md` section 3.1, Slice 0k-BM | `regression_tests/original_vs_stage_semantic_oracle_contract.sh <compiler>` compares original Crystal and stage output for explicit semantic lines (`TYPE=`, `CONST=`, `UNION=`). Slice 0k-BM made this strict-green for the H6-core TypeValue surface; rerun it whenever language-visible type identity changes. | current | [FALSIFIABLE] |
| B4 | Produced `s2b` must compile a full-prelude tiny source through LLVM entry without worker-only masking, RSS-kill, or post-`lower_main` crash. | Slice 0k-BN/0k-BO/0k-BP/0k-CK/0k-CU/0k-DK/0k-DL/0k-DM/0k-DN/0k-DT/0k-DU / `00-bootstrap-contract.md` section 4.2 | `scripts/generated_stage_llvm_entry_classifier.sh` builds/accepts stage1 and produced `s2b`, then compiles a full-prelude tiny source with default LLVM workers and `ADAMAS_LLVM_WORKERS=1`; `REQUIRE_CLEAN=1` remains the future acceptance gate. After 0k-CU, the old `current_0k_bn_frontier` / O1 Set crash is consumed and `REQUIRE_CURRENT_O1=1 scripts/mir_optimization_container_frontier_classifier.sh` exits at the expected non-current boundary with `b4_classification=llvm_entry_failure_after_lower_main` and `workers1_exit139=0`. Slice 0k-DT adds the mode-local OS-RSS selector. Slice 0k-DU adds the current selected subphase gate: `REQUIRE_CLASSIFICATION=1 REQUIRE_SUBPHASE=1 scripts/generated_stage_workers1_mir_subphase_classifier.sh` must select `classification=select_workers1_mir_optimization_resource_lane`, with earlier workers=1 HIR/MIR stop gates clean and MIR optimization memory-killed. A next production slice must target the selected workers=1 MIR optimization resource lane and preserve default late LLVM residual before worker/resource/output/tail/backend behavior changes. | current | [FRONTIER] |

## 3. HIR Name Resolution

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| H1 | Qualified nested names must not duplicate owner segments. | `01-hir-name-resolution.md` section 2 | `regression_tests/p2_qualified_module_namespace_no_prelude.sh <compiler>` finds `Float::Float::ParsedNumberStringT` or duplicated `Float::FastFloat`. | current | [FALSIFIABLE] |
| H2 | Self-reopen wrappers must not recursively register the current owner. | `01-hir-name-resolution.md` section 2.2, LM-553 | `regression_tests/p2_self_nested_module_registration_frontier.sh <compiler>`. | current | [FALSIFIABLE] |
| H3 | Nested builtin annotations must remain top-level unless structurally nested. | `01-hir-name-resolution.md` section 3, LM-554 | `regression_tests/p2_full_prelude_generic_template_namespace_no_pollution.sh <compiler>`. | current | [FALSIFIABLE] |
| H4 | Type-literal name queries lower to literal strings, not static stubs. | `01-hir-name-resolution.md` section 4, LM-558 | `regression_tests/p2_type_literal_name_query_no_stub.sh <compiler>`. | current | [FALSIFIABLE] |
| H5 | Function body presence must distinguish real bodies from stubs. | `01-hir-name-resolution.md` section 6 | `regression_tests/hir_function_body_presence_contract.sh` proves a registered bodyless HIR function is not `has_function_with_body?` and HIR->MIR preserves it as an empty unreachable stub, while real body evidence lowers as body. | current | [FALSIFIABLE] |
| H6 | Type-visible runtime values must share one HIR-owned `TypeValue` / `RuntimeTypeIdentity` fact for the H6-core surface. | `01-hir-name-resolution.md` section 4.1, B3 frontier | `regression_tests/type_value_core_runtime_identity_contract.sh <compiler>` compares original Crystal and stage output for direct/interpolated `typeof(1)`, direct/interpolated multi-arg `typeof(1, "x")`, direct/interpolated `1.class`, local and parenthesized nilable `.class`, and type-literal `.name` / `.to_s` / `inspect`; it deliberately excludes the parser-confounded no-parens command-call row. The wider historical guard remains `regression_tests/type_value_runtime_identity_contract.sh <compiler>`. | current | [FALSIFIABLE] |
| H7 | No-parens command calls must preserve a parenthesized member-access argument as the argument, not as member access on the command-call result. | 0k-BF / 0k-BG parser-frontier split | `regression_tests/command_call_member_access_preservation_contract.sh` asserts `puts (true ? 1 : nil).class` parses as a command call whose argument is `.class`; current parser is measured-red under `ADAMAS_EXPECT_COMMAND_CALL_MEMBER_MISMATCH=1`. | pre-s2-clean | [FRONTIER] |
| H8 | Runtime `.class` on a dynamic multi-variant union must report the concrete runtime class, not the static union display name. | `01-hir-name-resolution.md` section 4.1, Slice 0k-BM residual | `regression_tests/type_value_dynamic_union_class_residual.sh <compiler>` compares original Crystal and stage output for direct/interpolated `.class` on `ARGV`-dependent `Int32 | String`; current stage is measured-red under `ADAMAS_EXPECT_DYNAMIC_UNION_CLASS_MISMATCH=1`. | pre-s2-clean | [FRONTIER] |

## 4. Generic Template Registration

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| G1 | Generic/container fixes must not use arbitrary depth caps. | `02-generic-template-registration.md` section 2 | Add a deep nested tuple/hash/array/proc oracle before changing demand or registration pruning code. Static diff review alone is not enough. | next-touch | [MISSING-FALSIFIER] |
| G2 | Empty or repeated generic owner names such as `Iterator::` or `Indexable::Indexable::...` are invalid. | `02-generic-template-registration.md` section 3 | Add trace/IR guard that fails on empty owner suffixes or repeated adjacent owner segments in generated stage. | next-touch | [MISSING-FALSIFIER] |
| G3 | Generic template and instance keys must be semantic keys, not rendered strings. | `02-generic-template-registration.md` section 3 | `regression_tests/generic_identity_key_contract.sh` proves `GenericTemplateKey` and `GenericInstanceKey` equality/hash use owner, source `DefIdentity`, declared type params, specialization args, and lexical owner rather than display/rendered names. | pre-s2-clean | [FALSIFIABLE] |
| G4 | Broad source-gated generic-template body scan is not an acceptable fix. | `02-generic-template-registration.md` section 5 | Reverted experiment regressed earlier around `Crystal::PointerLinkedList` / trace paths. | current | [REFUTED] |
| G5 | Produced `s2` full-prelude `puts 42` must get past the later generic/template registration frontier. | `TODO.md`, LM-559 | `ADAMAS_TRACE_CLASS_FRONTIER=1 scripts/run_safe.sh <produced-s2> 60 4096 /tmp/hello.cr -o /tmp/hello_bin`. | pre-s2-clean | [FRONTIER] |
| G6 | The current owner-cache carrier must materialize `Hash(UInt64, BlockOwner)#[]=` as a real body, not an undefined-extern abort stub. | `TODO.md`, LM-ARCH-0K-AW, Slice 0k-CC/0k-CD/0k-CE/0k-CF | `regression_tests/block_owner_index_assign_materialization_repro.sh <compiler>` finds a non-stub self-IR body for the current `BlockOwner` carrier. Slice 0k-CF adds the stronger transaction guard: `scripts/block_owner_materialization_transaction_availability_report.sh <compiler>` must report at least one exact/all-equal BlockOwner setter `[MAT_TX]`, joined `[MAT_EMIT]` rows with `body_present=1`, at least one non-stub LLVM body, and zero setter stubs. Fresh `bin/adamas` evidence reports `tx_rows=1`, `joined_emit_rows=7`, `body_present_rows=7`, `real_defs=1`, and `stub_defs=0`. Green G6 remains non-bootstrap evidence: B4/L6 still gate `s2b`/`s3b`. | current | [FALSIFIABLE] |
| G7 | Exact all-equal materialization calls must classify body availability before falling back to backend extern/stub emission. | Slice 0k-CW / Slice 0k-CX / `MaterializationTransaction` exact body availability | `scripts/generated_stage_transaction_edge_selection_report.sh` first selected the broad residual with `post_consumer_state=selected_consumed_by_contract_consumer`, `contract_mismatch_rows=0`, `residual_exact_missing_body_rows=14`, `residual_exact_missing_body_groups=9`, and `residual_selection_status=rejected_exact_missing_body_ambiguous`. Slice 0k-CX adds `scripts/generated_stage_exact_body_availability_classifier.sh`; fresh current-source stage1 evidence reports `classifier_classification=reached_tx_and_emit`, `residual_exact_missing_body_rows=14`, `residual_body_lifecycle_cause_kinds=1`, `selected_cause=created_body_backend_missing`, `selected_rows=14`, `classification=rejected_body_lifecycle_class_too_wide`, and 9 lifecycle groups. Direct per-method fixes, backend undefined-extern rescue, forwarders, requested-name forcing, broad rendering normalization, ambient-map policy changes, or `BlockOwner` rollback are falsifier failures. | current | [FRONTIER] |
| G8 | A generated-stage exact missing-body residual with `materialization_action=created_body` must prove post-lowering HIR body visibility before backend fixes are considered. | Slice 0k-CX / Slice 0k-CY / Slice 0k-CZ / `created_body_backend_missing` classifier | `scripts/generated_stage_created_body_visibility_classifier.sh` refines the 0k-CX result and currently reports `created_body_missing_visibility_rows=14`, `visibility_cause_kinds=1`, `selected_cause=state_in_progress_without_hir_function`, `selected_rows=14`, `classification=rejected_visibility_class_too_wide`, and 9 visibility groups. Slice 0k-CZ corrects the evidence boundary: those body visibility fields are logged before `lower_method(...)`, so they prove only that `created_body` is not body-present evidence. A sampled-method patch, backend rescue, or treating pre-lowering visibility as post-lowering body availability falsifies the SDD process. | current | [FRONTIER] |
| G9 | Function lowering state must not be treated as a function body availability proof. | Slice 0k-CY / Slice 0k-CZ / Slice 0k-DA / Slice 0k-DB / Slice 0k-DC / Slice 0k-DD / Slice 0k-DE / Slice 0k-DF / Slice 0k-DG / Slice 0k-DI / post-lowering `FunctionAvailabilityContract` | `scripts/generated_stage_created_body_visibility_classifier.sh` joins `[MAT_TX]`, `[MAT_DONE]`, and `[MAT_EMIT]`. Slice 0k-DC keeps producer-path evidence on the existing `[MAT_DONE]` row and selects `attempt_lowering_returned_no_hir_function__producer_instance_class_info_lower_method__created_none`, 14 rows across 9 groups. Slice 0k-DD through 0k-DG add `scripts/generated_stage_lower_method_terminal_classifier.sh`, a temp-source classifier with `[MAT_METHOD_ENTRY]`, `[MAT_METHOD_NAME]`, `[MAT_METHOD_CALL]`, final `completed_method`, and `[MAT_PRECALL]` checkpoints in copied source only. Slice 0k-DI consumes the selected scope-entry edge: fresh evidence from `STAGE1_COMPILER=/tmp/adamas_scope_entry_stage1 REQUIRE_REACHED=1 SAMPLES=8 scripts/generated_stage_lower_method_terminal_classifier.sh` reports `completion_classifier_classification=reached_tx_and_emit`, `method_call_rows=266`, `precall_rows=1330`, `method_entry_rows=356`, `method_name_rows=310`, `method_exit_rows=666`, `residual_rows=3`, `terminal_cause_kinds=1`, `terminal_groups=2`, `terminal_root_sized_groups=2`, `selected_cause=lower_method_terminal_abstract_method`, and `classification=eligible_lower_method_terminal_edge`. The former selected `lower_method_terminal_no_exact_after_tx_no_call` bucket disappears; residual rows traverse the full pre-call chain and join to `[MAT_METHOD_CALL]`. This completes the focused scope-entry guard but does not prove green `s2b`/`s3b`; the next movement must remeasure the bootstrap pressure gate before selecting another owner edge. A separate `MaterializationAttemptResult` storage/log surface, HIR-to-MIR consumer ledger, optional production `lower_method` trace object, treating abstract methods as sampled local fixes, backend lookup/emission, undefined-extern stubs, forwarders, requested-name forcing, ambient-map policy, `NamedTuple`/`Tuple` rendering, `BlockOwner` rollback, new result/consumer layers, another generic pre-call marker without a named owner edge, or production `lower_method` trace-object plumbing are invalid moves. | current | [FALSIFIABLE] |

## 5. MIR Call ABI

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| M1 | Exact static calls lower before receiver mutation. | `03-mir-call-abi.md` section 3 | `regression_tests/p2_stage2_static_call_named_llvm_no_prelude.sh <compiler>` emits fallback `@func1` or wrong arity. | current | [FALSIFIABLE] |
| M2 | Receiver calls include a runtime receiver; static calls do not. | `03-mir-call-abi.md` section 4 | Add MIR shape check for the static-call reducer: named callee, callee arity equals args, no synthetic receiver. | next-touch | [MISSING-FALSIFIER] |
| M3 | Null/missing HIR `TypeRef` is not an ordinary runtime object. | `03-mir-call-abi.md` section 6 | Add no-prelude MIR oracle covering a null `TypeRef` conversion path. | pre-s2-clean | [MISSING-FALSIFIER] |
| M4 | Debug value-location metadata is opt-in during bootstrap and not semantic. | `03-mir-call-abi.md` section 7 | Build with metadata disabled and verify semantic guards still pass. | current | [FALSIFIABLE] |

## 5a. MIR Optimization Invariants

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| O1 | Produced-stage MIR optimization must not read null or uninitialized compiler-runtime `Set`/`Hash` state while validating CopyPropagation local replacements. | Slice 0k-CK/0k-CL/0k-CM/0k-CN/0k-CO/0k-CP/0k-CQ/0k-CR/0k-CS/0k-CT/0k-CU/0k-CV/0k-CW/0k-DJ | `REQUIRE_CURRENT_CP_BROAD=1 scripts/hir_block_return_shape_census.sh` is the pre-0k-CU broad-scope falsifier; `REQUIRE_CURRENT_CU_CONTRACT=1 scripts/hir_block_return_shape_census.sh` is the post-0k-CU contract gate. Fresh post-0k-CU evidence reports `classification=current_0k_cu_block_call_return_contract_applied`, `candidate_multi_shape_keys=207`, `candidate_additional_return_shape_bodies=224`, `assigned_tail_multi_shape_keys=0`, `timed_cp_phase_keys=5`, `timed_cp_phase_nil_value_coexist_keys=0`, `timed_cp_phase_assigned_tail_passthrough_keys=1`, and `timed_cp_phase_set_return_keys=1`. `STAGE1_COMPILER=/tmp/adamas_0kcu_stage1 REQUIRE_CURRENT_O1=1 scripts/mir_optimization_container_frontier_classifier.sh` exits at the expected non-current boundary with `b4_classification=llvm_entry_failure_after_lower_main` and `workers1_exit139=0`, so the old `affected_block_ids` / `Set(UInt32)#includes?` O1 frontier is consumed. The new residual is post-`lower_main` RSS/resource pressure in both worker modes; default workers still show the parallel rand fallback. Any next production slice from this row must return to the Current Execution Board and name a new owner edge for that residual. A classifier-only patch, broad return-shape specialization, direct CopyPropagation guard, backend rescue, `timed_cp_phase` special-case, memory-budget acceptance, forced worker count, broad `NamedTuple`/`Tuple` rendering change, ambient-map policy change, or `BlockOwner` rollback remains a falsifier failure. | current | [FRONTIER] |

## 6. LLVM Emission

2026-07-03 note: Slice 0k-EE supersedes the L15 late function-emission
selector for the active generated-stage gate. L15 remains as historical
anti-regression evidence for the function-emission lane; L17 is the current
post-function-emission frontier.

2026-07-03 note: Slice 0k-EF classifies L17. Slice 0k-EH narrowed L18 to the
produced compiler's final `IO::Memory#bytesize` field-read edge, but Slice
0k-EI refutes that as the first bad transition: the produced compiler's
`LLVMIRGenerator.@output` reference is already null before cast/getter/field
access.

2026-07-03 note: Slice 0k-EJ refutes the 0k-EI finalization-null boundary as
the first bad transition. The active L18 edge is now the
`emit_functions_parallel` rescue fallback output restore: current `@output` is
populated before restore, but `saved_output` is present with `pos=0`, and
restoring it zeros the output before finalization.

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| L1 | LLVM backend must resolve MIR `FunctionId` to the named callee. | `04-llvm-emission.md` section 2, LM-559 | `regression_tests/p2_stage2_static_call_named_llvm_no_prelude.sh <compiler>` rejects `@func1`. | current | [FALSIFIABLE] |
| L2 | LLVM calls must not have empty return type spelling. | `04-llvm-emission.md` section 3, LM-559 | Same guard rejects `call  @`. | current | [FALSIFIABLE] |
| L3 | Emitted IR for the static-call reducer must pass `llc` when available. | `04-llvm-emission.md` section 6 | Same guard runs `llc -filetype=obj` if `llc` exists. | current | [FALSIFIABLE] |
| L4 | Hardcoded IO overrides should use generated accessors instead of unstable offsets. | `04-llvm-emission.md` section 4 | Add guard that changes/observes fd ivar layout and verifies IO overrides still emit correct fd load. | next-touch | [MISSING-FALSIFIER] |
| L5 | Generated-stage LLVM emission must have an explicit `LLVMEmissionSession` owner record and a convergence vector before behavior patches to worker policy, side-effect merge, tail stubs, output files, or resource acceptance. | Slice 0k-BQ/0k-BR/0k-BS/0k-BT/0k-BU/0k-BV/0k-BW/0k-DK/0k-DL/0k-DM/0k-DN | `REQUIRE_SESSION=1 REQUIRE_WORKER_PLAN=1 scripts/llvm_emission_session_source_shape_guard.sh` must report `source_shape=session_consumes_function_plan` and `worker_shape=session_consumes_worker_plan` for the consumed `function-list-inline` and `worker-policy-inline` edges. Slice 0k-BT adds a guardrail: a future side-effect, tail, output, or resource slice must prove a downstream consumer reads a vertical `LLVMEmissionSession` contract, not merely that the session gained fields/getters/tags. Slice 0k-BV made the side-effect source-shape extension executable. Slice 0k-BW makes `REQUIRE_SESSION=1 REQUIRE_WORKER_PLAN=1 REQUIRE_SIDE_EFFECT_CONTRACT=1 scripts/llvm_emission_session_source_shape_guard.sh` green with `side_effect_contract_shape=session_consumes_side_effect_merge_contract`, writer/merge contract calls `1/1`, and inline raw writer/merge counts `0/0`. That green source shape still does not satisfy bootstrap completion after 0k-DN: the current generated-stage gate is `final_classification=abort_resource_after_lower_main` with joined transaction rows and mode-divergent resource boundaries. A production behavior change in side-effect merge, tail stubs, output files, resource acceptance, metadata/DWARF/type-name, or worker policy without a selected transaction-owned mode-local resource lane remains a falsifier. | current | [FALSIFIABLE] |
| L6 | The next generated-stage LLVM-entry movement must be transaction-level and owner-spine aware, not another local session-edge, joined-row proxy, or report-only selector. | Slice 0k-BX/0k-BY/0k-BZ/0k-CA/0k-CB/0k-CC/0k-CF/0k-CG/0k-CH/0k-CI/0k-CJ/0k-CK/0k-DK/0k-DL/0k-DM/0k-DN/0k-DT/0k-DU/0k-DV/0k-DW | `scripts/generated_stage_execution_transaction_report.sh` must join invocation setup, function plan, worker/fallback policy, side-effect contract, tail declaration/stub inputs, output ownership, resource evidence, B4 final classification, LLVM generate phase, function-emission phase, and mode-local boundaries under one transaction id for the same produced-compiler run. After 0k-DN, `REQUIRE_JOINED=1 REQUIRE_POST_CU_RESOURCE=1 REQUIRE_RESOURCE_PHASE_SPLIT=1 REQUIRE_FUNCTION_EMISSION_SPLIT=1 REQUIRE_WORKER_MODE_BOUNDARY=1` is the measured-red transaction gate. After 0k-DW, `REQUIRE_CLASSIFICATION=1 REQUIRE_PHASE=1 scripts/generated_stage_workers1_copyprop_phase_classifier.sh` is the selected resource phase gate and must report `classification=select_workers1_copyprop_apply_build_dominators_resource_lane`, earlier CP phase cutoffs below threshold, and dominance-build memory-kill. Production behavior patches to workers, tail stubs, output semantics, metadata/DWARF/type-name, resources, side effects, backend forwarders, memory budgets, `ADAMAS_LLVM_WORKERS=1`, `NamedTuple`/`Tuple` rendering, ambient maps, parser behavior, materialization behavior, physical extraction, HIR lowering, MIR type registration, MIR prepare, MIR body lowering, earlier MIR optimization passes, earlier CopyPropagation phases, or `BlockOwner` remain invalid until the selected workers=1 dominance-build lane is consumed or refuted and the default residual is preserved. | current | [FALSIFIABLE] |
| L7 | The current default-worker post-`lower_main` resource residual must keep its joined function-emission boundary while the 0k-DR lane refutation prevents pipeline-retention patches from this memory evidence. | Slice 0k-DL/0k-DM/0k-DN/0k-DO/0k-DP/0k-DQ/0k-DR / `GeneratedStageExecution` phase cursor | `STAGE1_COMPILER=/tmp/adamas_worker_boundary_stage1 TAIL_LINES=30 REQUIRE_JOINED=1 REQUIRE_POST_CU_RESOURCE=1 REQUIRE_RESOURCE_PHASE_SPLIT=1 REQUIRE_FUNCTION_EMISSION_SPLIT=1 REQUIRE_WORKER_MODE_BOUNDARY=1 scripts/generated_stage_execution_transaction_report.sh` must report `resource.default_mode_boundary=reached_function_emission`, `resource.function_emission_split=during_sequential_function_emit`, `resource.function_emission_last_phase=sequential_progress`, and `resource.function_emission_last_index=80` / `resource.function_emission_last_total=150` for the current default-mode residual. Slice 0k-DO adds `REQUIRE_REFUTED=1 scripts/generated_stage_external_sink_preflight.sh` as the external-sink falsifier. Slice 0k-DP adds `REQUIRE_CURRENT=1 scripts/generated_stage_function_emission_memory_discriminator.sh`, expected `function_emission_preexisting_non_gc_pressure`. Slice 0k-DQ adds `REQUIRE_OWNER=1 scripts/generated_stage_pre_function_memory_owner_classifier.sh`, now superseded by 0k-DR. Slice 0k-DR adds `REQUIRE_SPLIT=1 scripts/generated_stage_pre_hir_memory_split_classifier.sh`, expected `pre_hir_pressure_compile_entry`, with produced-s2 first high `memory.phase` already at `cli.compile_entry` and stage1 workers=1 control at 0 non-GC. If the default gate reaches `sequential_done`, first-high phase moves after compile entry, stage1 control reports high non-GC, or memory rows disappear, the evidence surface decayed. Tail, stub, metadata, type-name, DWARF, output-file, memory-budget, rand, worker-policy, external-sink, incremental function-output, LLVM session/function-plan, parse/HIR retention, materialization, `NamedTuple`/`Tuple`, ambient-map, and `BlockOwner` fixes are invalid first moves while this gate remains current. | current | [FRONTIER] |
| L9 | The default-lane pre-HIR produced-stage non-GC split has been consumed and refutes parse/source/prelude/HIR/LLVM pipeline retention as a first owner edge. | Slice 0k-DR / `GeneratedStageExecution` memory-phase lane refutation | `REQUIRE_SPLIT=1 scripts/generated_stage_pre_hir_memory_split_classifier.sh` must report `classification=pre_hir_pressure_compile_entry`, `terminal_status=terminal`, `default_first_phase=cli.compile_entry`, `default_first_high_owner=cli.compile`, `default_first_high_non_gc` above threshold, `default_max_phase=cli.compile_entry`, and a clean stage1 workers=1 control (`stage1_max_non_gc=0`, stdout `42`). A future patch or selector that treats the old `pre_function_pressure_hir_owned` row as an active HIR-owner receipt fails this frontier unless it first refutes 0k-DR with fresh evidence. | consumed | [FRONTIER] |
| L10 | The 0k-DR compile-entry GC `non_gc` row must not be treated as actual startup RSS or as permission to patch startup/parse/HIR/MIR retention. | Slice 0k-DS / `GeneratedStageExecution` startup/process baseline | `REQUIRE_CLASSIFICATION=1 scripts/generated_stage_startup_resource_baseline_classifier.sh` must report `classification=llvm_or_later_resource_boundary`, low compile-entry OS RSS (`s2_compile_entry_peak_rss_mb` below `HIGH_RSS_MB`), low parse/HIR/MIR stop-gate RSS, and the nested full generated-stage memory kill after lower_main. A production change to startup, parse, HIR, MIR, GC accounting, memory budget, worker policy, or old `memory.phase` non-GC accounting before this stop-gate evidence decays fails the frontier. | current | [FRONTIER] |
| L11 | The next generated-stage resource behavior slice must target the mode-local lane selected by OS RSS, not the more convenient default function-emission stack. | Slice 0k-DT / `GeneratedStageExecution` mode-local resource lane | `REQUIRE_CLASSIFICATION=1 REQUIRE_LANE_SELECTION=1 scripts/generated_stage_mode_resource_lane_classifier.sh` must report `classification=select_workers1_hir_to_mir_resource_lane`, clean stage1 workers=1 MIR control, clean produced-s2 HIR stop gates in both modes, clean produced-s2 default MIR stop gate, produced-s2 workers=1 MIR stop-gate memory-kill, and joined transaction residuals for both modes. A production slice that patches default sequential function emission, worker/rand fallback, memory budgets, output, tail, backend semantics, materialization, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner` before consuming/refuting this workers=1 HIR-to-MIR lane fails the frontier. | current | [FRONTIER] |
| L12 | The selected workers=1 HIR-to-MIR resource lane must be split by OS RSS before a MIR behavior change, and current evidence selects MIR optimization rather than HIR/body lowering. | Slice 0k-DU / `GeneratedStageExecution` workers=1 MIR subphase lane | `REQUIRE_CLASSIFICATION=1 REQUIRE_SUBPHASE=1 scripts/generated_stage_workers1_mir_subphase_classifier.sh` must report `classification=select_workers1_mir_optimization_resource_lane`, low stage1 workers=1 MIR control RSS, clean produced-s2 HIR/type-registration/prepare/body stop gates, produced-s2 MIR optimization memory-kill, and full MIR memory-kill. A production slice that patches HIR lowering, MIR type registration, MIR prepare, MIR body lowering, default LLVM function emission, worker policy, memory budgets, backend rescue, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner` before consuming/refuting the workers=1 MIR optimization lane fails the frontier. | current | [FRONTIER] |
| L13 | The selected workers=1 MIR optimization resource lane must be consumed through CopyPropagation state/resource ownership, not another broad optimizer patch. | Slice 0k-DV / `GeneratedStageExecution` workers=1 MIR optimization pass lane | `REQUIRE_CLASSIFICATION=1 REQUIRE_PASS=1 scripts/generated_stage_workers1_mir_opt_pass_classifier.sh` must report `classification=select_workers1_mir_opt_copy_propagation_resource_lane`, low stage1 control RSS, clean produced-s2 MIR bodies, clean constant-folding/local-CSE/RC-elision cutoffs, copy-propagation memory-kill, and preserved later high cutoffs. A production slice that patches HIR/MIR lowering, constant folding, local CSE, RC elision, default LLVM function emission, worker policy, memory budgets, backend rescue, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner`, or that adds another generic optimizer selector before consuming/refuting CopyPropagation ownership, fails the frontier. | current | [FRONTIER] |
| L14 | The selected workers=1 CopyPropagation resource lane must be consumed through dominance-construction ownership, not affected-block collection, rewrite, broad CP tuning, or another generic selector chain. | Slice 0k-DW / Slice 0k-DX / `GeneratedStageExecution` workers=1 CopyPropagation dominance lane | `REQUIRE_CLASSIFICATION=1 REQUIRE_PHASE=1 scripts/generated_stage_workers1_copyprop_phase_classifier.sh` must report `classification=select_workers1_copyprop_apply_build_dominators_resource_lane`, low stage1/pass precondition controls, clean `run_collect_state`, `run_find_replacements`, and `apply_collect_affected_blocks` cutoffs, broad `run_apply_replacements` high, first inner high at `apply_build_dominators`, and preserved later high apply phases. Slice 0k-DX adds `REQUIRE_CLASSIFICATION=1 REQUIRE_DOMINATOR=1 scripts/generated_stage_workers1_copyprop_dominator_classifier.sh`, expected `classification=select_workers1_copyprop_dom_compute_dominance_info_resource_lane`, clean `build_def_maps` and `skip_check` cutoffs, and a `compute_dominance_info` memory-kill. Broad local-only and local-safe-subset CP policies are refuted: both regressed generated `s2` to `pre_llvm_entry_failure` / exit 139 before MIR optimization. Slice 0k-DY consumes this lane with exact lazy dominance queries; a fresh run must now decay to clean workers=1 MIR optimization or the evidence has regressed. A production slice that patches earlier CP state collection, replacement discovery, affected-block collection, rewrite blocks, HIR/MIR lowering, earlier MIR optimizer passes, worker policy, memory budgets, backend rescue, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner`, disables all dominance-dependent CP, drops the whole dominance-needed subset, or adds another cascaded selector before consuming/refuting `compute_dominance_info` ownership, fails the frontier. | consumed | [FRONTIER] |
| L15 | After 0k-DY, the active generated-stage resource frontier is default late LLVM/function emission, not workers=1 MIR CopyPropagation dominance. | Slice 0k-DY / Slice 0k-EB / `GeneratedStageExecution` late LLVM resource residual | `STAGE1_COMPILER=tmp/adamas_lazy_dom_stage1 STAGE2_BUILD_TIMEOUT=600 REQUIRE_CLASSIFICATION=1 REQUIRE_LANE_SELECTION=1 TAIL_LINES=30 scripts/generated_stage_mode_resource_lane_classifier.sh` must report clean produced-s2 HIR/MIR stop gates in both modes, clean workers=1 MIR optimization by implication, joined transaction residuals, and `classification=select_default_late_llvm_resource_lane`. Fresh hostile revalidation also supports this when using a separately verified generated `s2`: direct self-build exits 0 under the strict 4GB cap, `scripts/generated_stage_self_build_boundary_classifier.sh` reports `classification=self_build_after_mir_boundary`, and `GENERATED_S2=<verified-s2> ... scripts/generated_stage_mode_resource_lane_classifier.sh` reselects `select_default_late_llvm_resource_lane`. Slice 0k-EB adds the current owner-fact guard: the same mode selector now passes through `transaction.default_function_emission_outcome_rows` and `transaction.workers1_function_emission_outcome_rows`, expected nonzero when the generated-stage transaction reaches sequential function emission. A production slice that returns to CP dominance, disables replacements, patches worker policy, memory budgets, backend rescue, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner`, or treats the last outcome function as a per-method patch target before a root-sized owner-edge falsifier, fails the frontier. | current | [FRONTIER] |
| L16 | Generated-s2 self-build failures must be separated from produced-s2 lane evidence before replacing L15. | Slice 0k-DZ / `GeneratedStageExecution` generated-s2 build guard | `scripts/generated_stage_self_build_boundary_classifier.sh` must classify the stage1 self-build of `src/adamas.cr` through compile-entry, parse, HIR, and MIR stop gates. Current evidence with `tmp/adamas_l16_stage1` reports `classification=self_build_after_mir_boundary`, with clean gates at `4`, `330`, `1722`, and `2328` MB. A direct full self-build under `scripts/run_safe.sh <stage1> 600 4096 src/adamas.cr -o <s2>` must either pass or provide the self-build boundary before a nested `s2_build_fails` row is used to retire L15. A production slice that treats a one-shot generated-s2 build failure as a late LLVM/function-emission refutation without this guard fails the frontier. | current | [FALSIFIABLE] |
| L17 | After 0k-EE, the active generated-stage frontier is post-function-emission finalization, not late function emission. | Slice 0k-EE / `GeneratedStageExecution` finalization boundary | A clean stage1 plus produced `s2` full-prelude `puts 42` run with `ADAMAS_GSETX_FUNCTION_EMISSION_OUTCOMES=1` must emit all planned sequential functions (149 in the fresh 0k-EE gate), reach `llvm.function_emission_phase=sequential_done`, `llvm.generate_phase=function_emission_done`, and then `llvm.generate_phase=finalize_to_s_enter`, with no late function-emission memory kill. Current evidence exits 139 at `finalize_to_s_enter` and peak RSS about 1.25 GB. The next production slice must split that finalization boundary, distinguishing `@out.to_s` / output stringification, metadata/DWARF/type-name, final write/link, and tail behavior before any output/tail/string-buffer patch. A patch that returns to #87/#92, `system_write`, worker policy, memory budget, backend rescue, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner` from this evidence fails the frontier. | current | [FRONTIER] |
| L18 | The generated-stage output-ownership/fallback-restore edge is consumed; do not return to finalization or generic `IO::Memory` from stale rescue-restore evidence. | Slice 0k-EF / `LLVMFinalization` finalize-to-s classifier; Slice 0k-EG / `IO::Memory` negative control; Slice 0k-EH / raw final-buffer micro-split; Slice 0k-EI / pre-cast output receiver split; Slice 0k-EJ / parallel rescue saved-output split; Slice 0k-EK / OutputOwnershipContract pre-code gate; Slice 0k-EL / `LLVMOutputOwnershipContract` behavior slice | `REQUIRE_OUTPUT_OWNERSHIP=1 scripts/llvm_output_ownership_source_shape_guard.sh` reports `output_ownership_shape=output_ownership_contract_consumed_by_parallel_restore`, with zero parallel direct saved-output snapshots/restores. Fresh generated-stage evidence with `STAGE1_COMPILER=tmp/adamas_output_owner_stage1 REQUIRE_RAW_DUMP=1 REQUIRE_CLASSIFICATION=1 scripts/generated_stage_finalize_to_s_classifier.sh` reports `classification=post_to_s_frontier`, `normal_finalize_to_s_done_rows=1`, matched rescue positions (`normal_parallel_rescue_current_pos_before_restore`, `normal_parallel_rescue_saved_output_pos`, and `normal_parallel_rescue_restored_output_pos` all nonzero and equal in the fresh run), and `raw_dump_classification=raw_dump_before_to_s_buffer_valid`. A future patch that reopens output ownership, finalization, generic `IO::Memory`, tail, metadata, DWARF, type-name, fd/raw output, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner` from pre-0k-EL evidence fails this frontier. | consumed | [FRONTIER] |
| L19 | After 0k-EN, the `String::HEADER_SIZE` scalar-global producer edge is consumed and must not be reopened from stale `ptr null` / `i32 0` evidence. | Slice 0k-EL / `LLVMOutputOwnershipContract`; Slice 0k-EM / post-`to_s` classvar scalar global discriminator; Slice 0k-EN / `offsetof` pending re-evaluation plus exact scalar `MacroNumberValue` constructors | `STAGE1_COMPILER=<stage1>` with `REQUIRE_RAW_DUMP=1 REQUIRE_CLASSIFICATION=1 scripts/generated_stage_finalize_to_s_classifier.sh` must report `normal_string_header_size_global_shape=i32_12`, `normal_string_header_size_global_line=@String__classvar__HEADER_SIZE = global i32 12`, and `raw_dump_classification=raw_dump_before_to_s_buffer_valid`. Adjacent guards `regression_tests/p2_constant_globals_no_prelude.sh <stage1>`, `regression_tests/p2_prescan_complex_constants_frontier.sh <stage1>`, and `regression_tests/p2_macro_number_parsed_literals_no_prelude.sh <stage1>` must pass. A future patch that returns to source fallback, broad classvar defaults, `MacroNumberValue` constructor churn, `String::Builder` arithmetic, backend undefined-global fallback, output ownership, finalization, tail, metadata, DWARF, type-name, worker policy, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner` from pre-0k-EN evidence fails this frontier. | consumed | [FRONTIER] |
| L20 | After 0k-EN, the current post-`to_s` generated-stage residual is LLVM IR type validity after the scalar-global producer is proven correct. | Slice 0k-EN / classvar scalar global producer fix | `STAGE1_COMPILER=<stage1>` with `REQUIRE_RAW_DUMP=1 REQUIRE_CLASSIFICATION=1 scripts/generated_stage_finalize_to_s_classifier.sh` currently reports `classification=post_to_s_llc_type_mismatch_frontier`, `normal_string_header_size_global_shape=i32_12`, and an `llc` mismatch where `%r18.fromslot.1` is defined as `i64` but expected as `ptr`. The next production slice must pin the producer of that i64-vs-ptr value after confirming `String::HEADER_SIZE` remains scalar-correct; it must not treat the moved classification as permission to patch constant globals, source fallback, macro-number constructors, output ownership, finalization, tail, metadata, DWARF, type-name, worker policy, `NamedTuple`/`Tuple`, ambient maps, or `BlockOwner`. | current | [FRONTIER] |
| L8 | The current workers=1 resource residual is a separate HIR-final-to-MIR-final boundary and must not be hidden behind a default-mode function-emission fix. | Slice 0k-DM/0k-DN / `GeneratedStageExecution` mode-boundary cursor | The strict report above must keep `resource.workers1_mode_boundary=after_hir_final_before_mir_final`, `runtime.workers1_hir_final_rows=1`, `runtime.workers1_mir_final_rows=0`, and `runtime.workers1_function_emission_phase_rows=0` until a later slice selects or moves this workers=1 lane. A patch that claims bootstrap progress from default-mode `sequential_progress` without preserving the workers=1 HIR-to-MIR residual fails this falsifier. | current | [FRONTIER] |

## 7. CLI Output

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| C1 | Emit-only success does not prove normal binary output success. | `06-cli-output-contract.md` section 2 | Same reducer passes `--emit llvm-ir --no-link` but normal `-o <bin>` exits 139. | current | [FALSIFIABLE] |
| C2 | Post-LLVM tail fixes must localize crash after LLVM finalization. | `06-cli-output-contract.md` section 4, LM-564 | `regression_tests/p2_stage2_cli_output_tail_no_prelude.sh <compiler>`; a future tail fix lacking the section 7 localization log is invalid evidence. | current | [FALSIFIABLE] |
| C3 | Binary-output fix must verify adjacent modes. | `06-cli-output-contract.md` section 5 | A commit claims binary-output fix with only `--emit llvm-ir --no-link` evidence. | current | [FALSIFIABLE] |

## 8. Refuted Branches

| ID | Branch | Evidence | Status |
|----|--------|----------|--------|
| R1 | Source-first generic type-param extraction as a broad fix. | Did not move the Float/ParsedNumberStringT frontier. | [REFUTED] |
| R2 | Caching `node.body` as a broad generic-template registration fix. | Did not move the frontier. | [REFUTED] |
| R3 | Source-gating generic-template nested-type body scan. | Failed earlier around `Crystal::PointerLinkedList` / trace paths. | [REFUTED] |
| R4 | Re-enabling source-backed top-level return annotations after LM-558. | Regressed produced `s2` full-prelude `puts 42` to earlier class registration crash around `class register idx=51/104`. | [REFUTED] |

## 9. Bootstrap Investigation Process

| ID | Claim | Source | Smallest Falsifier | Phase | Status |
|----|-------|--------|--------------------|-------|--------|
| P1 | Trace absence is not enough to prove a function was not entered. | LM-565 | A tail/root claim relies only on a missing trace line when lldb, breakpoint, or IR control-flow evidence is practical. | current | [FALSIFIABLE] |
| P2 | Small helpers in self-host critical paths are semantic risk until s1->s2 proves otherwise. | LM-565 | A helper/refactor in compiler bootstrap code is committed with only host or no-prelude evidence after touching a produced-stage path. | current | [FALSIFIABLE] |
| P3 | Sidecar model output is candidate evidence, not acceptance evidence. | LM-565 | A fix adopts Cursor/Grok/Spark claims without local reproduction or a guard. | current | [FALSIFIABLE] |
| P4 | Bootstrap cache/IO/hash code is runtime surface, not harmless infrastructure. | LM-564, LM-565 | A cache or filesystem-tail change lacks a produced-stage guard covering the path that consumes the cache/hash result. | next-touch | [FALSIFIABLE] |
| P5 | A gate-local root fix must name deeper roots that remain open. | LM-564, LM-565 | LANDMARK/TODO wording claims a subsystem is fixed when the evidence only clears a current bootstrap gate. | current | [FALSIFIABLE] |
| P6 | A stashed or local WIP is not completion evidence after an architecture pause. | Slice 0k-BL | A future production slice resumes a WIP without a fresh measured-red baseline, owner-edge statement, producer/consumer map, and residual boundary. | current | [FALSIFIABLE] |
| P7 | A generated-stage classifier is architecture work only when it answers an owner-boundary question. | Slice 0k-BP/0k-BQ | A future B4 classifier extension only narrows a crash marker or offset and lacks a `PhaseAuthority` / `GeneratedStageExecution` owner question, producer/consumer map, old authority edge, and residual boundary. | current | [FALSIFIABLE] |
| P8 | A report-only slice after a report-only stop rule is architecture work only if it retires, refutes, or replaces a prior report surface with a code-owned authority edge plan. | Slice 0k-AP/0k-CG/0k-CH | A future slice adds another selector/report/counter for B4/L6 but does not retire/refute an older report surface and does not name a `GeneratedStageExecutionOutcome` owner fact/service plus one old authority edge and consumer migration target. | current | [FALSIFIABLE] |
| P9 | A phase-outcome owner implementation is valid only when a source consumer migrates to it under a guard while preserving the measured-red behavior boundary. | Slice 0k-CH/0k-CJ | `REQUIRE_OUTPUT_OUTCOME=1 scripts/generated_stage_outcome_source_shape_guard.sh` must report `source_shape=outcome_serializes_output_commit_rows` and zero direct output rows outside outcome helpers. A future commit that changes `GeneratedStageExecutionOutcome`, output row names/values, or the CLI output corridor must keep this guard green and must not change B4/L6 behavior without an admitted behavior slice. | current | [FALSIFIABLE] |
| P10 | Behavior-neutral owner migrations are architecture progress only when they decrease `BootstrapPotential`, not merely a local source-shape/proxy metric. | Slice 0k-CI | A future source slice after the current 0k-CH candidate does not state `BootstrapPotential = (B4/L6 phase, plausible owner-spine count, live proxy-surface count, unmigrated authority-edge count)` before edits, or it only decreases `unmigrated authority-edge count` while B4/L6, owner-spine ambiguity, and live proxy surfaces stay unchanged, then selects another behavior-neutral edge instead of SDD redesign, owner-spine refutation, direct root localization, or an explicit `bootstrap-emergency-with-ledger` behavior slice. | current | [FALSIFIABLE] |
| P11 | A B4 root-localization slice may redirect the active owner spine only when it names a direct runtime stack and the next discriminating classifier, not when it merely narrows a marker or crash offset. | Slice 0k-CK | A future slice uses the `Set(UInt32)#includes?` / `CopyPropagationPass` stack to patch `CopyPropagation`, backend Set/Hash delegates, LLVM workers/resources/output/tail, or broad namespace/container rendering without first classifying the bad Set/Hash producer versus malformed optimization state. | current | [FALSIFIABLE] |
| P12 | The 0k-CR B4/O1 emergency lane must not become the default next-symptom loop. | Slice 0k-CS | After the paired assigned-tail passthrough + return-shape wrapper-materialization slice lands, or if it widens beyond the root-sized assigned-tail class, a future commit that adds another crash-stack classifier or local fix without an Active Architecture Board decision is invalid. Valid follow-on decisions must select a durable owner spine, retire/refute a stale report surface, promote a missing contract falsifier from this matrix, or explicitly admit a new `bootstrap-emergency-with-ledger` slice. | current | [FALSIFIABLE] |
| P13 | The active architecture plan must be executable from the short `Current Execution Board`, not reconstructed from historical append-only ledger text. | Slice 0k-CT | A future non-doc production slice is invalid if it cannot cite a `SliceReceipt` from `docs/compiler_architecture_sdd.md` naming board lane, tranche, old authority edge, owner fact/service, producers, consumers, measured-red baseline, focused and architecture DoD, generated-stage gate, negative controls, rejected shortcuts, and residual boundary. A slice that only says "latest crash moved" or selects work from historical `0k-*` ledger paragraphs without the short board is a process falsifier failure. | current | [FALSIFIABLE] |
| P14 | An unfinished production WIP after an architecture pause is not progress evidence and must not be resumed by inertia. | Slice 0k-CV | A future source commit continues the quarantined 0k-CU helper path, or any analogous uncommitted helper WIP, without first updating the Current Execution Board receipt, naming the old authority edge consumed, and running the declared generated-stage gate. A source-shape-only green row, partial helper introduction, or moved crash stack is insufficient. | current | [FALSIFIABLE] |
